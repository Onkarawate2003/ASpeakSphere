<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Dev-only: allow plaintext HTTP to the LAN IP the FastAPI backend
        runs on (see frontend/.env.local NEXT_PUBLIC_API_BASE_URL) and to
        the Android emulator's host-loopback alias. Android 9+ (API 28+)
        blocks cleartext traffic by default; this app targets API 36.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.25</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
